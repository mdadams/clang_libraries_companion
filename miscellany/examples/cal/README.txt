Clang Auxiliary Library (CAL)
=============================

The Clang Auxiliary Library (CAL) provides a collection of utility code
(e.g., functions and classes) that may be helpful when using the
Clang/LLVM libraries to process C/C++ source code.

The API of this library is not guaranteed to be stable.
